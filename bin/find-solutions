#!/usr/bin/env bash
find ~/fetch/ -maxdepth 1 -type d -name "solution_*" \
  # Find top solutions
  -exec find {}/top/ -maxdepth 1 -type f -name "*.pdb" \; 2> /dev/null \
  # Find all solutions
  -exec find {}/all/ -maxdepth 2 -type f -name "*.pdb" \; 2> /dev/null
