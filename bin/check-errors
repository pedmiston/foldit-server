#!/usr/bin/env bash
WORKLOAD_DIR=$1
BUNDLE_ID=$2
DST=${WORKLOAD_DIR}/errors-${BUNDLE_ID}.csv
cat ${WORKLOAD_DIR}/*errors.csv > ${DST}
rm ${WORKLOAD_DIR}/*errors.csv
N_ERRORS=$(wc -l <${DST})
if N_ERRORS == 0; then
  rm ${DST}
fi
