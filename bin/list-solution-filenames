#!/usr/bin/env python
import sys
import json
import unipath

assert len(sys.argv) == 2, 'must provide data dir'
data_dir = unipath.Path(sys.argv[1])

for run in data_dir.listdir('run-*.json'):
    for j in open(run, 'r'):
        d = json.loads(j)
        sys.stdout.write(d['FILEPATH'][0]+'\n')
